# Local development (matches compose.yaml). Point this at Neon or any
# managed Postgres 16+ for deployment.
DATABASE_URL=postgresql://setwise:setwise@localhost:5433/setwise

# Set to "require" when connecting to a hosted Postgres that needs SSL
# (Neon, RDS, etc). Leave unset for local Docker Postgres.
DATABASE_SSL=
