## What changed

## Why

## How to verify
- [ ] `npm run lint`
- [ ] `npm run typecheck`
- [ ] `npm run build`
- [ ] `npm run db:migrate` against a local Postgres
- [ ] `npm run db:seed` (if the catalog or schema changed)
- [ ] `npm test`
